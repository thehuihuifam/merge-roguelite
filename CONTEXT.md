# CONTEXT.md — 1분 온보딩

## 한 줄 정의

**Merge Roguelite** 는 숫자 공을 떨어뜨려 같은 숫자끼리 합치는 물리 기반 머지 게임에, "충돌 순간의 슬로우모션 선택 · 리스크 카드 · 근접 실패 연출" 이라는 로그라이트 긴장감을 얹은 웹게임이다.

## 현재 버전과 상태

| 항목      | 값                                                                                                                                      |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| 버전      | **v0.2.0** (차별화 메커니즘 1차 완료 · v0.3.0 로그라이트 구조 진행 중)                                                                  |
| 상태      | 코어 루프 + 차별화 메커니즘 1차(슬로우모션 선택, 리스크 카드, 근접 실패 연출) + 라운드 시스템 + 폭탄 공 + 라운드 HUD + 스폰 압박 리스크 카드(HEAVY LOAD)까지 완료                        |
| 미구현    | 세이브, 사운드, 파티클, 모바일 QA, 배율 카드 지속 시간 관리(`ModifierStack`) — 실제 구현은 코드에 있으나 런 피드백 연결이 진행 중(PROGRESS.md 2.20~ 참조) |
| 확장 방식 | `src/core/interfaces/` 의 인터페이스를 구현한 **새 파일 추가** 로 확장 (기존 코드 수정 최소화)                                          |
| 스택      | TypeScript (strict) · Vite · Matter.js · Vitest · ESLint · Prettier · Node 20 LTS                                                       |

명령: `npm run dev` / `npm test` / `npm run lint` / `npm run typecheck` / `npm run build`

## 이 프로젝트의 정신 — 왜 이 게임이 재밌는가

1. **물리적 불확실성이 만드는 가변 보상.** 같은 자리에 떨어뜨려도 공이 구르고 튕기며 예상 밖의 연쇄 합체가 터진다. 플레이어는 "계획"과 "운" 사이에서 매 드롭마다 작은 도박을 한다.
2. **선택이 있는 머지.** 기존 머지 게임은 떨어뜨리는 것 외에 결정이 없다. 우리는 충돌 순간에 0.4초 슬로우모션을 걸고 카드 3장을 내밀어, "지금 이 합체를 어떻게 굴릴 것인가" 라는 즉각적 결정을 요구한다.
3. **함정이 섞인 보상.** 카드 3장 중 1장은 리스크 카드다. 큰 배율 뒤에 위험선 상승이나 큰 공 스폰 같은 대가가 붙는다. 욕심과 안전 사이의 줄타기가 런마다 다른 이야기를 만든다.
4. **거의 죽을 뻔한 순간을 연출한다.** 공이 위험선에 닿기 직전 화면이 붉어지고 시간이 느려진다. 살아남으면 안도감, 죽으면 "한 번만 더" 가 된다.

## 다음에 읽을 문서 순서

1. `CONTEXT.md` (지금 이 문서)
2. `docs/GDD.md` — 게임 디자인: 코어 루프, 도파민 설계, 차별화 메커니즘 스펙, 승패 조건
3. `docs/ARCHITECTURE.md` — 폴더 구조, 데이터 흐름, 확장 포인트, 상태 머신
4. `PROGRESS.md` — 지금 할 태스크 (`Active Next Action`)
5. 필요 시 `docs/DECISIONS.md`, `docs/ROADMAP.md`, `AGENTS.md`

## 절대 하지 말아야 할 것

- **Suika(수박게임) 클론으로 퇴화하지 말 것.** "떨어뜨리고 합치기" 만 다듬는 작업은 이 프로젝트의 목표가 아니다. 모든 기능은 3대 차별화 메커니즘(슬로우모션 선택 · 리스크 카드 · 근접 실패 연출) 또는 로그라이트 구조(라운드 · 카드 · 런 변주)에 기여해야 한다.
- 기존 확장 포인트 인터페이스(`src/core/interfaces/`)의 메서드를 **삭제하거나 시그니처를 깨지 말 것.** 필드 추가는 허용, 제거는 별도 논의.
- `src/physics/` 밖에서 `matter-js` 를 import 하지 말 것. 물리 엔진 교체 가능성을 유지한다.
- 코어 로직(`src/core/`)에 DOM/Canvas 의존성을 넣지 말 것. 코어는 Node 환경의 Vitest 에서 그대로 돌아가야 한다.
- 한 PR 에서 태스크 2개 이상을 처리하지 말 것. 테스트 삭제로 CI 를 통과시키지 말 것.
